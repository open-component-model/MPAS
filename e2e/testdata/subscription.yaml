apiVersion: delivery.ocm.software/v1alpha1
kind: ComponentSubscription
metadata:
  name: podinfo
  namespace: mpas-system
spec:
  component: mpas.ocm.software/podinfo
  semver: 1.0.0
  source:
    url: http://registry.ocm-system.svc.cluster.local:4000/mpas-component
  serviceAccountName: mpas-admin
  verify:
  - name: default
    secretRef:
      name: mpas-sig
